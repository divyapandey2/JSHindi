//Arrays in JavaScript can contain various types of data, such as numbers, strings, objects, and even other arrays (nested arrays).

// Array literal
let fruits = ['apple', 'banana', 'cherry'];

// Using Array constructor
let numbers = new Array(1, 2, 3, 4, 5);

// Empty array
let emptyArray = [];

// Array with values

let number= [1, 2, 3, 4, 5];
let mixedArray = [1, 'Hello', true, { name: 'John' }];

