//includes Method-
//The includes method is used to check whether an array includes a certain value among its elements. It returns true if the array contains the specified element, and false otherwise.

Syntax:

array.includes(searchElement[, fromIndex])

//Example
let numbers = [1, 2, 3, 4, 5];

console.log(numbers.includes(3)); // Output: true
console.log(numbers.includes(6)); // Output: false
